# Not used: this project's test suite runs on the standard-library
# `unittest` module (see _pathfix.py), not pytest, because the sandbox
# this was developed in has no internet access to install pytest.
# Kept as an empty file rather than deleted for tooling compatibility.
